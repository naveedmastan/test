import React from "react";

const SingleBlock = React.createContext({});

export const SingleBlockProvider = SingleBlock.Provider;
export const SingleBlockConsumer = SingleBlock.Consumer;
export default SingleBlock;
